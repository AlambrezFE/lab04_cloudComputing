<!DOCTYPE html>
<html>
<head>
    <title>Productos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f0f0f0;
        }
        .producto {
            border: 1px solid #333;
            padding: 1em;
            margin: 1em;
            width: 50%;
            text-align: center;
        }
        a {
            display: inline-block;
            margin: 1em;
            padding: 0.5em 1em;
            text-decoration: none;
            background-color: #007BFF;
            color: white;
            border-radius: 5px;
        }
        a:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Productos</h1>
    <% productos.forEach(producto => { %>
        <div class="producto">
            <p>Nombre: <%= producto.nombre %>, ID: <%= producto.id %></p>
        </div>
    <% }); %>
    <a href="/">Regresar</a>
</body>
</html>
